import { FunctionOptions } from '../commandProcessing';
export default function edit(args: FunctionOptions) {
  
}

// This should open local editor, enable you to edit file with vim / nano / code / ..., and after upload the temp file again to the server